<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="mProgress_circle">
        <item name="android:indeterminateDrawable">@drawable/anim_loading</item>
        <item name="android:minWidth">140dp</item>
        <item name="android:minHeight">140dp</item>
        <item name="android:maxWidth">140dp</item>
        <item name="android:maxHeight">140dp</item>
    </style>
    
    <style name="NoTitleDialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/loading_bg</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    
    <style name="mReminder_circle">
        <item name="android:indeterminateDrawable">@drawable/anim_reminder</item>
        <item name="android:minWidth">90px</item>
        <item name="android:minHeight">90px</item>
        <item name="android:maxWidth">90px</item>
        <item name="android:maxHeight">90px</item>
    </style>
    
    <style name="ReminderDialog" parent="android:style/Theme.Dialog">
		<!-- 背景颜色及透明程度 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否有标题 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否模糊 -->
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>
</resources>